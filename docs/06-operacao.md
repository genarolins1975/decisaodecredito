# Operação: execução, backup, restauração, atualização, rollback, observabilidade e custos

## 1. Executar

```bash
cp .env.example .env            # ajuste DATABASE_URL, APP_SECRET, APP_URL
npm ci
npm run db:migrate              # aplica drizzle/*.sql
npm run db:seed                 # curso, edição 2026, turma 2026-A, professor (senha inicial só em desenvolvimento)
node scripts/content/extract.mjs && node scripts/content/build-legacy.mjs && npm run content:import
npm run dev                     # http://localhost:3000
npm run build && npm start      # produção
```

Contas de teste do seed (apenas fora de produção): professor `genaro.lins@gmail.com` / `professor-dev-2026`; aluno.a, aluno.b, monitor, sem.matricula e aluno.outra em `@example.test`.

Variáveis: ver `.env.example`. `APP_SECRET` cifra tokens OAuth e assina URLs curtas; trocá-la invalida a conexão Gmail (reconectar) e nada mais.

## 2. Backup

```bash
DATABASE_URL=... STORAGE_LOCAL_DIR=./storage scripts/backup.sh backups/
```

Gera `db-<data>.dump` (pg_dump formato custom), `files-<data>.tar.gz` (armazenamento local) e `manifest-<data>.sha256`. Com S3, use o versionamento e a replicação do provedor para os arquivos. Frequência sugerida: diário automático e imediatamente antes de cada aula e de cada publicação de notas. Guarde cópias fora do servidor.

## 3. Restauração (ensaio obrigatório em ambiente isolado)

```bash
createdb curso_restauracao
RESTORE_DATABASE_URL=postgres://.../curso_restauracao scripts/restore.sh backups/db-<data>.dump backups/files-<data>.tar.gz
```

Ensaio executado em 16/09/2026 nesta sessão: ver `docs/07-relatorio-de-testes.md`, seção "Backup e restauração". Verifique após restaurar: contagem de páginas (180), usuários, matrículas e último `audit_log`.

## 4. Atualização e rollback

- Atualização: `git pull`, `npm ci`, `npm run db:migrate`, `npm run build`, reiniciar. Migrações são aditivas; nunca edite uma migração aplicada.
- Rollback de aplicação: reimplantar a tag anterior. Como as migrações são aditivas, a versão anterior continua funcionando com o esquema novo.
- Rollback de conteúdo: Conteúdo → página → Versões → "publicar esta" na versão anterior.
- Rollback de dados: restaurar backup em banco novo e apontar a aplicação para ele (janela de manutenção).

## 5. Observabilidade e alertas

- `GET /api/health` responde `{ok:true}` com banco acessível: use em monitor externo (ex.: verificação a cada minuto).
- Fila de e-mail: a tela E-mail mostra falhas; Início mostra a contagem de mensagens com falha.
- Trilha de auditoria: tabela `audit_log` (ações de autenticação, matrícula, sessão, frequência, notas, arquivos). Alunos não podem apagar eventos.
- Logs da aplicação: erros internos vão para stderr com prefixo `[api]`; não registram senhas, tokens nem conteúdo acadêmico.
- Limpeza: `cleanupOrphans()` em `src/lib/services/files.ts` marca uploads pendentes com mais de 24 h; agende uma chamada diária (rota administrativa a criar) ou execute por script.

## 6. Custos de operação

Ver `docs/01-arquitetura-e-decisoes.md`, seção 6: opção recomendada ≈ US$ 45 por mês (Vercel Pro + Supabase Pro), preços lidos em 16/09/2026. Nenhum serviço pago foi contratado nesta sessão.

## 7. Publicar as bases do trabalho final

As bases, os gabaritos e o gerador ficam fora do repositório (ele é público). O pacote vai ao bucket privado sem passar pela Vercel (limite de 4,5 MB por requisição) e é registrado no catálogo em duas etapas separáveis, para que a máquina que gera o pacote não precise da senha do banco:

```bash
# 1) upload: só as variáveis do armazenamento (STORAGE_DRIVER=s3, S3_ENDPOINT, S3_REGION, S3_BUCKET, S3_ACCESS_KEY_ID, S3_SECRET_ACCESS_KEY)
npx tsx --tsconfig scripts/tsconfig.json scripts/dados/publicar.ts /caminho/do/pacote --modo upload
# 2) registro: pelo botão "Registrar pacote" em Bases e gabaritos ou, com DATABASE_URL, pelo script
npx tsx --tsconfig scripts/tsconfig.json scripts/dados/publicar.ts /caminho/do/pacote --modo registrar --edicao 2026
```

O diretório do pacote traz `manifesto.json` (versão, sha256 e tamanho de cada arquivo) e, por base: pacote do aluno (zip com desenvolvimento, dicionário e README), dicionário (csv), OOT sem desfecho (csv), rótulos do OOT (csv) e pacote do professor (zip com gabarito, verdades e métricas). Os objetos ficam em `bases/v<versao>/`. O registro confere existência e tamanho de cada objeto, grava `files` com chave estável e sha256 do manifesto, atualiza o catálogo (`datasets`: arquivo, dicionário, OOT, rótulos, gabarito, versão, status disponível), registra os materiais comuns (pacote do trabalho, publicado; gabaritos consolidados, só professor) e habilita o teste cego do trabalho final em cada turma da edição. Rótulos e gabaritos têm finalidade `labels`: só o professor baixa. Idempotente: registrar de novo atualiza no lugar; uma nova versão usa outro prefixo e o botão com a nova versão. Um manifesto só com materiais comuns (`bases: []`) atualiza apenas os materiais: o mesmo material em nova versão substitui o anterior no lugar (o título é comparado sem o sufixo de versão). Arquivos pequenos podem ser enviados pelo painel do R2 (pasta `bases/v<versao>/`), sem token.

## 7.1. Guias de capítulo em PDF (aluno e professor)

Um PDF por capítulo e versão, página a página. Cada página traz a captura da plataforma (peça nativa ou corpo do conteúdo; abertura e síntese impressas por inteiro), o texto autoral **Como ler esta página** (aluno) ou **Como conduzir esta página** (professor), o que está na tela, as questões, inclusive as curadas (com gabarito e diagnóstico só na versão do professor) e, na versão do professor, a ficha **Na hora da aula** (tempos, pergunta para a turma, resposta esperada, erros previsíveis, transição e a ponte de sala). A versão do professor abre com **Em sala**, a folha das essenciais com a ponte para a próxima; nas aulas com mais de um capítulo, as duas versões trazem **O fio da aula**. Os guias do aluno dos capítulos 4, 5 e 6 estão publicados em `content/materiais/` e são servidos por `/api/materiais/[arquivo]`; os guias do professor saem pelo bucket privado, com `scripts/apostila/pacote-professor.mjs` e "Registrar pacote" (detalhes em `scripts/apostila/README.md`). Os textos autorais vivem em `scripts/apostila/explicacoes/cNN.json` (padrão de voz em `c01.json`, regras no README da pasta, validação por `validar-explicacoes.mjs`). Procedimento, com o servidor local no ar e a conta de teste do aluno:

```bash
export APOSTILA_DIR=tmp/apostila
python3 scripts/apostila/figuras.py && python3 scripts/apostila/infograficos.py      # figuras conceituais e infográficos (SVG)
node scripts/apostila/captura-visuais.mjs todos                                        # 180 capturas (peça nativa ou conteúdo), 2x
node scripts/apostila/validar-explicacoes.mjs                                          # cobertura, tamanhos e caracteres proibidos
node scripts/apostila/gerar.mjs aluno todos --pdf && node scripts/apostila/gerar.mjs professor todos --pdf
python3 scripts/apostila/prancha.py tmp/apostila/build/capitulo-04-aluno.pdf           # prancha de conferência visual
```

`dump-capitulo.mjs N` imprime o conteúdo de um capítulo em texto para escrever ou revisar as explicações. A capa de cada capítulo é o infográfico de abertura gerado de `content/infograficos/cNN.json` por `infograficos.py`, a mesma fonte do componente da plataforma e do palco. Duas gramáticas: a de blocos (quatro cartões, três números com fórmula, três painéis, faixa de três ideias) e a de fluxo (`"variante": "fluxo"`, capítulo 4: três etapas com setas, painel das escalas com a curva logística, painel do coeficiente e faixa de limite); `tests/infograficos.test.ts` confere a forma dos onze arquivos e a aritmética do capítulo 4 contra os coeficientes da aula. A versão do aluno pode ser publicada como material comum; a versão do professor só como material com status "professor" (só o professor vê) e nunca no repositório, que é público. Com "Capítulo N" no título, o material aparece na página de abertura do capítulo. Regenerar sempre que o conteúdo for reimportado ou uma peça nativa mudar.

## 7.2. Visuais nativos das páginas (peças de assinatura)

Alguns visuais herdados (iframe do motor original) foram substituídos por componentes nativos, animados e responsivos, em `src/components/visuais/`. O registro em `src/components/visuais/registro.tsx` mapeia o slug da página para o componente; quando existe, o bloco `legacy` da página é trocado pelo visual nativo em aula, apresentação e aula ao vivo, sem alterar o banco.

- Capítulo 1 completo: `c1p7`: "Mesma PD" (`mesma-pd.tsx`, duas operações com a mesma PD e resultados esperados opostos, R$ 1.007 contra −R$ 852, com a PD na mão e as parcelas). Capítulo 3 completo: `c3p15`: "Balancear" (`balancear.tsx`, reponderação monótona das odds: AUC 0,725685 com qualquer proporção artificial, PD média de 9,72% a 45,34%, e a correção de volta); `c3p16`: "WoE" (`woe.tsx`, a composição da faixa na mão e as seis faixas de utilização do treino); `c3p17`: "Valor da informação" (`valor-da-informacao.tsx`, seis faixas de renda com contribuição para o IV, total 0,03174, fraca). Dados em `src/lib/visuais/woe.json`, gerados pelo mesmo script. Com isso nenhuma página usa o motor legado em iframe: as 88 páginas com visual são nativas. `c1p5`: "Cem vidas em doze meses" (`cem-vidas.tsx`). Simulação com risco mensal constante; sem dados externos.
- Capítulo 2 completo: `c2p2`: "Recorte" (`recorte.tsx`, a linha do tempo com o corte na data da decisão, cada informação testada contra o corte, o fluxo até a PD de 13%); `c2p5`: "Variáveis" (`variaveis.tsx`, quatro campos da mesma proposta com papel, tipo, operações e armadilha temporal, mais a matriz de operações por tipo); `c2p6`: "Mesmas características" (`mesmas-caracteristicas.tsx`, 2 pessoas, 16 e a retirada de um caso, com o intervalo de Wilson de cada amostra); `c2p7`: "Condicional" (`condicional.tsx`, quatro condições sobre as 16 propostas com o plano e a fronteira: 57,5% dá 7 em 8 contra 1 em 8). `c2p11` e `c2p12`: "A bolinha na perda" (`bolinha-na-perda.tsx`, modos perda e descida; `src/lib/visuais/perda.ts`: perda de uma PD constante com 1 default em 8, mínimo 0,3768 em 12,5%, descida em b com passo escolhido), `c2p14`: "A árvore que decora" (`arvore-que-decora.tsx`, árvore do capítulo 5 sem freio: profundidade 1 erra 2 com 2 folhas, profundidade 3 zera o erro com folhas de uma proposta, entre elas a #15) e `c2p16`: "O intervalo que encolhe" (`intervalo-que-encolhe.tsx`, Wilson: 1 em 8 de 2,2% a 47,1%, 100 em 800 de 10,4% a 15,0%).
- Capítulo 10 completo: `c10p5` a `c10p9`: "Memorando do comitê" (`memorando.tsx`, um campo por página: texto, os cinco itens que a rubrica procura, medidor de completude com a regra dos cinco itens e 120 caracteres, mapa dos cinco campos e o material de cada um; o campo 1 lê a política congelada em `lab10.estado` e o campo 5 registra a cobertura dos cinco fenômenos; tudo guardado em `lab10.memo`, em `src/lib/visuais/lab-estado.ts`; material em `src/lib/visuais/memorando.json`, gerado pelo mesmo script a partir de `content/generated/dados.json`: AUC 0,7257 contra 0,6958, Wilson 8,93% a 13,45%, diferença de AUC 0,0299 com intervalo de 0,0001 a 0,0596, grupos 4,7 pp com intervalo de −2,5 a 11,9 pp, PSI 0,0136). `c10p2`: "Três clientes, três zonas" (`tres-zonas.tsx`, cadeia PD, valor e ação com corte e teto na mão), `c10p3`: "O quadro de decisão do comitê" (`quadro-do-comite.tsx`), `c10p11`, `c10p12` e `c10p13`: "A mesa do comitê" (`mesa-sob-choque.tsx`, modos rodada1, choque e rodada2; estado gravado no navegador em `lab10.estado` e compartilhado pelas três páginas). Motor de política em três zonas em `src/lib/visuais/politica.ts` (porte do motor original: revisão manual com capacidade, sinal ruidoso da PD verdadeira do gerador com qualidade 0,6, R$ 90 por caso), conferido: corte 12%, teto 30% e capacidade 80 dão 548 aprovados, R$ 607 mil e 36 defaults entre aprovados; sob o choque (funding 21%, perda 80%, PD +0,30 em log odds) dão 461 aprovados e −R$ 225 mil. `oot-logistica.json` ganhou a PD verdadeira (`pt`).
- Capítulo 11, `c11p6`: "O modelo perfeito que está errado" (`modelo-perfeito.tsx`: linha do tempo com a disponibilidade de cada campo, regra do capítulo 3 aplicada ao campo candidato, e os resultados do gerador em `vazamento.json`: boosting honesto AUC 0,6958, vazamento suave 0,9676, campo do horizonte 1,0000 com Brier 0,00028) e `c11p18`: "Avaliação final: quatro perguntas" (`quatro-perguntas.tsx`, notas de 0 a 2 e total em 8).
- Capítulo 3, `c3p7`: "A linha do tempo do cliente" (`linha-do-tempo.tsx`) e `c3p11`: "A base amadurece" (`safras.tsx`). Regras em `src/lib/visuais/tempo.ts` (disponibilidade contra evento; maturação com horizonte 12 e apuração 1, que reproduz a partição da base: fora do tempo até dez 2023). Entram no lugar da figura estática (`substitui: "figura"`), preservando o texto ao redor.
- Capítulo 4 completo: `c4p2`: "A reta que quebra" (`reta-que-quebra.tsx`, figura), `c4p9`: "Três escalas, um ponto" (`tres-escalas.tsx`) e `c4p19`: "A fronteira nasce" (`fronteira.tsx`); `c4p3` a `c4p6`: "As escalas" (`escalas.tsx`, modos probabilidade, odds, logodds e regua: cem quadrados e a parede em 0 e 1, odds contra PD com a tabela de leitura, dobrar e reduzir à metade com ±0,6931 em log odds, e as quatro réguas lado a lado com o escore didático 600 − 90 × z, PD 5% dá 865); `c4p7`: "A curva logística" (`curva-logistica.tsx`: z −1,50 dá PD 18,2%, somar 1 leva a 37,8%, inclinação local 0,1492); `c4p13`: "O intercepto" (`intercepto.tsx`: a fila de 16 desliza inteira sem trocar de lugar, PD média 50,2% com β₀ = −5,65); `c4p15` a `c4p17`: "A descida completa" (`descida-completa.tsx`, modos perda, gradiente e descida: perda por proposta com #2 em 1,3223 e média 0,43282; uma iteração em β = 0 com gradiente (0; −0,59375; −0,23438) e as 16 contribuições; descida em blocos de +1 a +10.000 com perda 0,693147 → 0,432824 e as três trajetórias); `c4p21`: "Faixas" (`faixas.tsx`: quatro faixas de contagem igual com intervalo de Wilson nas 16 propostas e na base de treino do gerador, 2.103 propostas com o desfecho agora em `escores.json`, contra a logística contínua por Newton). Regressão logística em `src/lib/visuais/logistica.ts` sobre as 16 propostas didáticas (`did.json`), conferida contra o gerador: descida com passo 0,1 chega a β = (−5,6666; 0,7453; 1,3955) em 20.000 iterações, #11 dá PD 56,18%, corte de 50% recusa 8 com 6 defaults evitados, mínimos quadrados negativos abaixo de 12,8% de utilização.
- Capítulo 5 completo: `c5p3`: "Anatomia" (`anatomia.tsx`, diagrama compartilhado em `arvore-diagrama.tsx` com cada elemento aceso ao toque e a conta de 2 elevado a d folhas); `c5p4` e `c5p5`: "Impureza" (`impureza.tsx`, modos curva e raiz: Gini e entropia contra a proporção, o quadrado da chance de errar, e o Gini da raiz 0,50000 ao lado de uma folha pura e de uma mista); `c5p6`: "Corte candidato" (`corte-candidato.tsx`: utilização ≤ 62,5% dá 0,34568 e 0,24490, média ponderada 0,30159, ganho 0,19841); `c5p9`: "Recursão" (`recursao.tsx`: os dois nós de oito com seus quatro melhores candidatos e o plano de cada um); `c5p11`: "Caminho" (`caminho.tsx`: 72% e 8 dias caem na folha 6 de 6, PD 100%, contra 69,3% da logística); `c5p15`: "Poda" (`poda.tsx`: impurezas 0,5, 0,21875, 0,125 e 0 recalculadas, trocas em 0,0547 e 0,2813, quatro folhas nunca vence); `c5p18`: "Duas famílias" (`duas-familias.tsx`: reta contra escada no plano, log loss 0,43282 contra 0,18844 com piso e teto). Também `c5p7`, `c5p14` e `c5p16`: "A árvore que cresce" (`arvore-que-cresce.tsx`, modos raiz, freios e instabilidade). Árvore em `src/lib/visuais/arvore.ts` (Gini, candidatos nos pontos médios, empate decidido pela ordem utilização e atraso), conferida contra o gerador: raiz utilização 57,5 com ganho 0,28125, nível 2 em 27,5 e 87,5, freios 2 e 2 com 4 folhas e 2 erros, retirar a #10 troca o nó direito para atraso.
- Capítulo 6 completo: `c6p2`: "Três estratégias" (`tres-estrategias.tsx`: selecionar, combinar em paralelo e corrigir em sequência, com o passo a passo de x = 8 sobre os oito pontos, previsão de 6,50 a 10,86); `c6p15`: "Hiperparâmetros" (`hiperparametros.tsx`: η, M, profundidade e mínimo por folha na base didática, log loss 0,47481 contra 0,43282 da logística na configuração da aula, 16 folhas somadas, PD de 33% a 67%); `c6p18`: "Três limites" (`tres-limites.tsx`: sobreajuste com a grade do gerador, instabilidade com a primeira árvore sem a #15 e sem a #16, explicação com as parcelas da proposta #11). `c6p7`, `c6p8` e `c6p9`: "Resíduos que encolhem" (`residuos-que-encolhem.tsx`, modos taxa, arvores e soma) e `c6p12`, `c6p13` e `c6p14`: "A perda que cai" (`perda-que-cai.tsx`, modos alvo, iteracoes e rastro); `c6p17`: "A distância que se abre" (`distancia-que-se-abre.tsx`, figura, dados em `grid-boosting.json`). Boosting em `src/lib/visuais/boosting.ts` (árvore de regressão por soma de quadrados, F0 = média ou log odds da prevalência, F = F + η h), conferido contra o gerador: regressão com η 0,5 e cortes 4,5 · 7,5 · 2,5 · 5,5, MSE 10,1875 → 0,4485; classificação com η 0,4, profundidade 2 e folha mínima 2, perda 0,6931 → 0,4748, rastro da proposta 12 em +0,200 · +0,183 · +0,165 · +0,152 com PD 66,8%; η × árvores até perda 0,50 perto de 1,5.
- Capítulo 7, `c7p6`: "A fila de risco" (`fila-de-risco.tsx`); `c7p2`: "O acerto que engana" (`acerto-que-engana.tsx`); `c7p5`: "AUC é uma contagem de pares" (`pares.tsx`); `c7p7` e `c7p8`: "KS e ganho por decil" (`ks-e-decis.tsx`, modos ks e ganho); `c7p9` e `c7p10`: "Calibração por faixa" (`calibracao-por-faixa.tsx`, modos grupos e faixas); `c7p11` e `c7p12`: "Brier e log loss" (`brier-e-logloss.tsx`, modos deslocamento e recalibrar); `c7p16` e `c7p18`: "Três amostras" e "Decisão e evidência" (`evidencias.tsx`). Avaliação em `src/lib/visuais/avaliacao.ts` (matriz de confusão, decis de risco com Wilson, ganho e alavancagem, faixas de calibração, Brier, log loss, deslocamento em log odds, pares da fila didática), conferida contra o gerador: corte 12% com 48/164/33/492 e acerto 73,27% contra 89,01%; 12 de 20 pares e 53.136 pares na janela; KS 0,3621 e separação 0,3426 no corte; dois decis com 42,0% (logística) e 37,0% (boosting); faixas de calibração iguais às do gerador; Brier 0,09128 e log loss 0,31487 com AUC imóvel sob deslocamento. Dados em `oot-modelos.json` (PD do boosting bruta e calibrada, ganhos, calibração, Platt).
- Capítulo 9 completo: `c9p2`: "Três fenômenos" (`tres-fenomenos.tsx`, a assinatura esquemática de cada fenômeno nas três leituras e a tabela de diagnóstico); `c9p7`: "Gatilhos" (`gatilhos.tsx`: sete gatilhos com limiar, janela, responsável e ação, medição da janela só depois da leitura: PSI 0,0136, maior índice por característica 0,0260 na utilização, razão 0,8845, alta de AUC 0,0849, grupos 4,7 pp com intervalo que inclui zero, dois não instrumentados, nenhum disparo); `c9p8`: "Painel" (`painel.tsx`, onze candidatos em `src/lib/visuais/painel.ts`, linha do tempo de quando cada um avisa, diagnóstico de cobertura e seleção guardada em `lab9.painel`, lida pelo campo 5 do memorando). `c9p3`, `c9p4` e `c9p5`: "O índice que soma" (`indice-que-soma.tsx`, modos faixas, variaveis e leituras) e `c9p6`: "Equidade depende da pergunta" (`equidade.tsx`). Monitoramento em `src/lib/visuais/monitoramento.ts` (decis congelados no treino, PSI com piso 0,0001, janela simulada por deslocamento e mistura de canal, intervalo de Wald para diferença de proporções, as quatro perguntas de equidade), conferido contra o gerador: faixas 731,0 · 763,9 · … · 908,9, PSI 0,0133 aqui contra 0,0136 no gerador (empates no limite com escores de uma casa, declarado na peça), aprovação 72,7% contra 68,0% com IC de −2,5 a 11,9 pp, prevalência +1,43 pp com IC de −1,15 a +4,02 pp. Dados em `escores.json` (2.103 escores de treino e 737 da janela) e `monitoramento.json`.
- Capítulo 8, `c8p8` e `c8p11`: "A curva de lucro" (`curva-de-lucro.tsx`); `c8p3`, `c8p5` e `c8p6`: "Resultado por proposta" (`resultado-por-proposta.tsx`, modos fracao, parcelas e equilibrio: R$ 350 na operação de R$ 10 mil, ponto de equilíbrio 13,76% e a tabela de cenários); `c8p7`: "A troca do corte" (`troca-do-corte.tsx`: 55 defaults recusados com R$ 606 mil evitados, 213 bons com R$ 330 mil abandonados, saldo R$ 276 mil); `c8p9`: "Revisão manual" (`revisao-manual.tsx`: o calculado sobe e o valor real, sob a PD verdadeira do gerador, cai); `c8p10`: "Ponte" (`ponte.tsx`, corte 14%: 580 aprovados, R$ 608 mil, folga zero); `c8p12`: "A política que fecha" (`politica-que-fecha.tsx`, composição por faixa de PD usada e congelamento em `lab10.estado`, lido pelo capítulo 10). O motor de política ganhou valor real, aprovados na revisão e fila não atendida; o estado do laboratório vive em `src/lib/visuais/lab-estado.ts`. Motor econômico em `src/lib/visuais/economia.ts` (parâmetros de c8p5 e c8p6, choque em log odds de c8p11), conferido contra o gerador: corte de 10% com 469 aprovados e R$ 585 mil, máximo em 14% com R$ 608 mil, realizado de R$ 378 mil na janela. Palpites da turma digitados pelo professor viram marcas na curva. Usa `src/lib/visuais/oot-logistica.json`, gerado por `node scripts/content/visuais-dados.mjs` a partir de `content/generated/dados.json` (737 propostas fora do tempo e PD da logística).

Os cálculos ficam em `src/lib/visuais/metricas.ts` (funções puras) e são conferidos contra o gerador em `tests/visuais.test.ts` (AUC 0,7257, KS 0,3621 em PD 9,74%, corte de 12% com 48 de 81 e 164 de 656). Regras das peças: botão de reproduzir e controle único, cores por papel (default em `--color-alert`, pagou em azul claro, corte em `--color-gold`), números tabulares, respeito a `prefers-reduced-motion`, layout por container query e variante compacta dentro do slide (`.slide-inner .vz`); a seção "Palco" de `globals.css` dá a cada peça alturas em `cqh` (fração da altura do slide) e esconde painéis secundários, e `AjusteAoPalco` aplica um zoom entre 0,6 e 1 se ainda assim a peça não couber.

## 7.4. Página do capítulo e limites de erro

`/aulas/capitulo/N` (`src/app/(app)/aulas/capitulo/[n]/page.tsx`) monta o cartão de visita do capítulo a partir de `chapterOverview` (conteúdo publicado da edição da turma ativa), dos prerrequisitos derivados, do infográfico em `content/infograficos/cNN.json` e dos materiais cujo título cita o capítulo (`materiaisDoCapitulo`). A primeira página de aula não repete o infográfico; o palco (`/apresentacao`) continua abrindo o capítulo com ele. Limites de erro (`error.tsx`) nas áreas do aluno e do professor mantêm a moldura e exibem o digest; `not-found.tsx` em `/aulas/[slug]`, `/aulas/capitulo/[n]` e `/professor/conteudo/[pid]` explicam o motivo provável (edição da turma sem a página publicada; identificador desatualizado).

### Laboratório de regressão logística (c4p2)

`src/components/visuais/lab-logistica.tsx` com as contas em `src/lib/visuais/lab-logistica.ts` (funções puras, testadas em `tests/lab-logistica.test.ts`). Convenção: x é a utilização em proporção (70% = 0,70); eixo e controles em porcentagem. O ajuste da aula é recalculado sobre as 16 propostas de `did.json` a cada carga: reta por mínimos quadrados (a = −0,142647, b = 1,117647) e logística por máxima verossimilhança via Newton (β₀ = −3,121787, β₁ = 5,429194). Mover um controle é exploração de parâmetros e marca o cenário como "Parâmetros exploratórios"; "Restaurar ajuste da aula" volta aos dois modelos estimados. O modo "Comparar com a reta" usa a e b próprios, sem passar pela logística e sem truncar. Os quatro experimentos guiados fazem a pergunta e só revelam a explicação no botão. Ponto arrastável no gráfico, setas do teclado (Shift para 10 pp), campos numéricos em formato brasileiro e tela cheia pela Fullscreen API.

### Escala 1, probabilidade (c4p3)

`src/components/visuais/escala-probabilidade.tsx` com as contas em `src/lib/visuais/escala-probabilidade.ts` (testadas em `tests/escala-probabilidade.test.ts`). PD por operação em passos de 1 pp com campo numérico, grade de exatamente cem quadrados (20 × 5) cuja contagem coincide com a expectativa 100 × p, três réguas do incremento (5%, 50% e a PD selecionada) na mesma escala de 0% a 120% sem limitar o resultado a 100%, e a faixa de odds com os extremos tratados (0 em p = 0, sem valor finito em p = 1). No palco a peça recebe `palco` e começa na etapa Interpretar a PD; Testar +10 pp, Revelar odds, Mostrar tudo e Restaurar são botões; no estudo começa com tudo à vista. Os visuais nativos passam a receber a propriedade opcional `palco` do `ContentBlocks`.

### Escala 2, odds (c4p4)

`src/components/visuais/escala-odds.tsx` com as contas em `src/lib/visuais/escala-odds.ts` (testadas em `tests/escala-odds.test.ts`). Controle de 1% a 99% em passos de 1 pp, campo de PD com decimais e campo de odds, ambos validados (negativos, acima de 100%, não numéricos) e com aviso quando o valor real sai da faixa do controle; odds = p ÷ (1 − p) com os extremos (0 em p = 0, sem valor finito em p = 1); gráfico da função verdadeira com eixo fixo de 0 a 20, indicação de "fora da janela" e ajuste de escala opcional; comparação entre p e 1 − p (odds recíprocas) e ponte para log odds, ambas reveladas pelo professor no palco e à vista no estudo.

### Escala 3, log odds (c4p5)

`src/components/visuais/escala-logodds.tsx` com as contas em `src/lib/visuais/escala-logodds.ts` (testadas em `tests/escala-logodds.test.ts`). Uma PD de partida (controle de 1% a 99% em passos de 1 pp, campo com decimais, atalhos 10%, 33%, 50% e 80%, restauração em 33%) gera três cenários calculados sempre a partir das odds iniciais, nunca em sequência: odds ÷ 2, partida e odds × 2. As três colunas trazem a PD em destaque e odds e log odds como valores secundários, com o deslocamento em pontos percentuais e em log odds. Duas réguas alinhadas mostram o que a tabela esconderia: a de PD é fixa de 0% a 100% e a de log odds usa a janela de leitura de −6 a +6, que contém todos os cenários de 1% a 99% e não é limite da escala. Os segmentos respeitam as distâncias numéricas exatas, sem ampliação para legibilidade; os rótulos que não cabem somem da régua (os valores permanecem nas colunas). Em PD = 50% o texto declara que os passos também são iguais, com os valores calculados; nos demais casos mostra os deslocamentos reais, sem afirmação estática de desigualdade. À direita, a identidade ln(2 × odds) = ln(odds) + ln(2); no rodapé, a pergunta sobre a partida em 50% com revelação reversível, restaurada pelo Restaurar. A aba "Ver a função" troca as réguas pelo gráfico completo de logit(p) com os três pontos sincronizados e legenda fixa no canto livre. Cada régua é desenhada em duas geometrias, a larga da leitura e da projeção e a compacta do celular (menos rótulos, texto maior em unidades do viewBox); o container query escolhe qual aparece e o palco força a larga, porque a regra genérica `.conteudo figure svg` mostraria as duas.

### Abertura do capítulo 4 em dois quadros (c4p1)

`src/components/visuais/logit-slides.tsx` desenha dois quadros 16:9 (`.rl-slide`, sistema visual próprio, tamanhos em cqw sobre a referência 1920 × 1080) a partir das contas em `src/lib/visuais/logit-slides.ts` (testes em `tests/logit-slides.test.ts`): o primeiro com as características como controles e coeficientes fixos, o segundo com a transformação +10 pp → +0,7453 em z → × 2,11 nas odds e as quatro réguas de partida. Entra com `substitui: "abertura"` (antes de todos os blocos) e, no palco, como página inteira: `src/lib/visuais/palco-proprio.ts` lista as páginas cujo visual desenha o próprio quadro; para elas a moldura do slide não mostra cabeçalho, título nem o infográfico de abertura, e cada quadro é uma tela. Os visuais nativos recebem `pagina` (posição no capítulo) para numerar os quadros.

### Decomposição do escore (c4p8)

`src/components/visuais/escore-soma.tsx` com as contas em `src/lib/visuais/escore-soma.ts` (testes em `tests/escore-soma.test.ts`), no mesmo sistema de quadro 16:9 do c4p1. Um só estado, utilização e atraso, alimenta a tabela de parcelas (coeficiente, valor na escala com a divisão por 10 explícita e contribuição), a faixa da soma e as três representações do resultado: escore em log odds, odds = e^z e PD = odds ÷ (1 + odds). A soma usa precisão integral e nunca parcelas já arredondadas; o teste compara as duas somas para garantir isso. Entra com `substitui: "pagina"`, modo novo do registro: o visual ocupa o lugar de todo o conteúdo herdado da página (restam apenas as questões, montadas fora do `ContentBlocks`), e `PALCO_PROPRIO` inclui `c4p8`, de modo que no palco o quadro é a página inteira. O bloco herdado que saiu trazia um diagrama de sete variáveis para uma fórmula de duas, o selo "recalculado aqui" e dois parágrafos longos; no palco, o compositor o dividia em três telas e a tabela ficava separada da frase do resultado, o que deixava uma tela quase vazia. Regras do conteúdo herdado (`.conteudo th`, `.conteudo thead th`, `.conteudo input[type="range"]`) alcançam o visual dentro do palco: as regras da tabela e dos controles são escritas com o prefixo `.rl` para ter precedência.

### Leitura do coeficiente (c4p10)

`src/components/visuais/coeficiente-pd.tsx` com as contas em `src/lib/visuais/coeficiente-pd.ts` (testes em `tests/coeficiente-pd.test.ts`), quadro 16:9 no sistema `.rl`. A página deixa de ser expositiva e vira verificação: enunciado fixo (utilização de 70% para 80%, atraso mantido em 5 dias, β = 0,7453 por unidade de 10 pp), três interpretações em `fieldset` com rádios reais e um painel à direita que troca de conteúdo entre orientação, resposta e o detalhe sobre manter constante, sem mudar a altura do quadro. A resposta só aparece depois de Conferir resposta; o retorno traz ícone, texto e cor, e a demonstração calculada pelo modelo (Δx = 1, Δz = 0,7453, escore 0,2483 → 0,9936, PD 56,18% → 72,98%, variação +16,80 pp). Como `substitui: "pagina"` passa a ocupar o lugar de todo o conteúdo herdado, a questão `c4p10q`, que era o mesmo exercício, sai do fluxo; a checagem de fim de página (`c4p10-checagem`), montada fora do `ContentBlocks`, continua. Nota de implementação: `fieldset` com `display: grid` agrupa os filhos não legenda em uma caixa anônima no Chromium, de modo que as linhas do grid não alcançam os cartões; a lista de alternativas fica em um `div` próprio dentro do `fieldset`.

### Razão de odds (c4p11)

`src/components/visuais/razao-de-chances.tsx` com as contas em `src/lib/visuais/razao-de-chances.ts` (testes em `tests/razao-de-chances.test.ts`), quadro 16:9 no sistema `.rl`. Exercício de três alternativas sobre a nova PD depois de multiplicar as odds: 21,1% (multiplicar a PD), 19,0% (o caminho correto) e 12,1% (somar 2,11 pontos percentuais). O multiplicador é `OR = exp(β)` com a precisão integral do coeficiente, e "2,11×" é exibição, com a nota correspondente; o teste compara o resultado com o de `OR = 2,11` exato (18,97% contra 18,99%) para garantir que as duas fontes não se misturam. O retorno aparece junto ao cartão escolhido, com ícone, texto e cor, e a conversão completa aparece à direita em qualquer caso: 0,1111, × 2,1071, 0,2341, 18,97%. A base traz o contraste entre o ganho em odds (cerca de 111%) e o deslocamento em PD (cerca de 9 pontos percentuais). Substitui todo o conteúdo herdado, inclusive a questão `c4p11q`, que era o mesmo exercício.

### Curva do impacto em PD (c4p12)

`src/components/visuais/impacto-pd.tsx` com as contas em `src/lib/visuais/impacto-pd.ts` (testes em `tests/impacto-pd.test.ts`), quadro 16:9 no sistema `.rl`. O experimento é fixo (+10 pp de utilização, Δz = β, m = exp(Δz)) e o aluno só muda a PD de partida, de 0,1% a 99,9%: controle, campo, gráfico, resultados e tabela saem do mesmo estado. A curva usa `p' = m p ÷ (1 − p + m p)`, que vale em p = 0 e p = 1 sem passar por odds infinitas, e é amostrada em 320 pontos mais o máximo. A revelação marca o máximo analítico, `p* = 1 ÷ (1 + √m)` e `Δ* = (√m − 1) ÷ (√m + 1)`, que dá 40,8% e 18,42 pp, contra 17,82 pp em 50%; o teste confirma que o máximo analítico coincide com o numérico e que 50% fica abaixo dele. A tabela de sete cenários é alternativa ao gráfico, com três colunas, e o maior valor é declarado como o maior entre os cenários listados, não como o máximo da curva. Duas armadilhas resolvidas aqui: a comparação de faixa precisa arredondar antes (99,9 ÷ 100 dá 0,9990000000000001 e cairia fora), e a regra genérica `.conteudo figure svg` do palco devolve a altura intrínseca do viewBox, de modo que o gráfico é posicionado por um invólucro `position: relative` com override de especificidade.

### Unidade do coeficiente (c4p14)

`src/components/visuais/unidade-coeficiente.tsx` com as contas em `src/lib/visuais/unidade-coeficiente.ts` (testes em `tests/unidade-coeficiente.test.ts`), quadro 16:9 no sistema `.rl`. A mesma proposta escrita em três unidades, por 10 pp, por 1 pp e como fração de 0 a 1: o coeficiente é multiplicado pelo mesmo fator que divide a variável, a contribuição βx não muda e o escore e a PD tampouco. A utilização é o único dado que o aluno altera; selecionar uma linha apenas destaca a representação e mostra a definição de x, sem tocar na proposta nem esconder as demais. O detalhe "E a razão de odds?" compara a mesma mudança real de +10 pp: Δx de 1, 10 ou 0,10 e o mesmo β × Δx = 0,7453, com OR ≈ 2,11; `exp(7,453)` fica de fora porque corresponde a 100 pp. Os testes verificam a coincidência das três contribuições em toda a faixa, a igualdade de escore e PD entre unidades e que a soma não usa parcelas já arredondadas. O intercepto permanece igual porque a troca de unidade não desloca a origem da variável, o que não vale para centralização.

### Log loss proposta a proposta (c4p15)

`src/components/visuais/log-loss.tsx` com as contas em `src/lib/visuais/log-loss.ts` (testes em `tests/log-loss.test.ts`), quadro 16:9 no sistema `.rl`. As 16 propostas de `src/lib/visuais/did.json` recebem PD pelos coeficientes da aula; a perda individual é calculada a partir do logit, na forma estável `ℓ = max(z, 0) − y z + log1p(exp(−|z|))`, sem arredondar a PD antes. O gráfico de barras mostra a perda de cada proposta na ordem original, com a média (0,43282) em linha tracejada e rótulo declarado como "log loss média do modelo", não como mínimo demonstrado. A cor identifica apenas a seleção. Cada barra é selecionável por clique e teclado, com alvo de largura inteira e foco visível; a seleção muda só o painel de inspeção, nunca as barras, a média ou os coeficientes. O painel usa a probabilidade do desfecho observado, que é PD quando y = 1 e 1 − PD quando y = 0, o que torna visível que PD alta também gera perda alta em quem não entrou em default. Os testes conferem a equivalência entre a forma pelo logit e a forma pela probabilidade, a estabilidade em logits extremos (onde a forma pela probabilidade satura), a média como soma sobre 16 e a monotonicidade em cada desfecho.

### Reta na probabilidade (c4p2)

`src/components/visuais/reta-na-probabilidade.tsx` com as contas em `src/lib/visuais/reta-na-probabilidade.ts` (testes em `tests/reta-na-probabilidade.test.ts`), quadro 16:9 no sistema `.rl`. A reta é a do projeto, por mínimos quadrados sobre as 16 propostas usando só utilização (`retaMinimosQuadrados`), e não os coeficientes da logística: p(u) = −0,1426 + 0,011176 × u, com u em porcentagem e p em proporção. O gráfico mostra a faixa válida, as linhas de 0% e 100%, os trechos fora do intervalo em vinho e o ponto móvel; o eixo vertical vai de −20% a 110% para não esconder a parte negativa. `forasDoIntervalo` só declara os trechos que existem: neste domínio há um (abaixo de 12,76% de utilização) e nenhum acima de 100%, já que p(100%) = 97,50%. O truncamento é sobreposto quando pedido, com `mín(1; máx(0; p))`, e a comparação de +10 pp calcula a diferença na reta e na função truncada, desabilitando-se quando o incremento sairia do domínio. **Esta página hospedava o laboratório interativo de regressão logística (`LabLogistica`)**, que passou para o c4p7 ("A curva logística faz o caminho de volta"), no lugar de `CurvaLogistica`; esse componente continua no repositório, sem página registrada.

### Uma iteração do gradiente (c4p16)

`src/components/visuais/gradiente-passo.tsx` com as contas em `src/lib/visuais/gradiente-passo.ts` (testes em `tests/gradiente-passo.test.ts`), quadro 16:9 no sistema `.rl`. Três faixas numeradas: o estado atual (em zero, PD de 50% em todas as propostas e log loss 0,69315), o gradiente como média sobre as 16 propostas, com as três fórmulas e a definição das variáveis (p: PD estimada; y: indicador de default; u: utilização em %; a: atraso em dias), e a atualização −ηg aplicada aos três parâmetros juntos, com η = 0,10. A coluna da direita mostra por padrão o painel de exemplo do coeficiente da utilização, com a conta por extenso (−0,10 × (−0,59375) ≈ +0,05938), a regra de sinal e o efeito no coeficiente; o gráfico das 16 contribuições, com a linha tracejada na média (que é o próprio g₁), passou para a expansão "Ver contribuições por proposta", e "Voltar ao exemplo" recompõe a coluna. A coluna da tabela é nomeada −ηg, e não gradiente. Aplicar recalcula PDs, perda e gradiente e mostra a perda antes e depois (0,69315 → 0,67194); a partir da iteração 1 o título da primeira faixa passa a "Estado atual — iteração t" e o indicador de PD troca de "PD de cada proposta" para "PD média estimada". Reiniciar restaura coeficientes, contador, perda anterior e a própria expansão. A exibição usa cinco casas com arredondamento simétrico (a magnitude arredonda meio para cima e o sinal volta depois), de modo que −0,234375 aparece como −0,23438. Saem a "distância até o ótimo" do conteúdo principal e a repetição do título dentro do painel.

## 7.3. Apresentação como deck: compositor e auditoria de layout

A apresentação (`/apresentacao/<slug>`) é um deck 16:9. Cada página vira uma sequência de telas compostas a partir das unidades do conteúdo (parágrafo, painel, figura, questão), inclusive dentro do HTML herdado do material. O compositor (`src/lib/palco/compositor.ts`, funções puras com testes em `tests/palco.test.ts`; lado DOM em `src/lib/palco/unidades.ts`) mede cada unidade com tudo visível e zoom 1 e escolhe o menor número de telas em que cada tela cabe na altura e não passa de 150 palavras; entre as partições com esse número, a que equilibra melhor as telas. Não abre tela nova se a ocupação média cairia abaixo de 40% antes do zoom. Cabeça (título, `.rot`, kicker) fica sempre com o corpo seguinte e legenda com a figura anterior. As colunas do material (`.palcoflex`) ficam lado a lado; quando uma coluna começa por figura e cabe folgada, ela persiste em todas as telas enquanto a outra é paginada; uma coluna sozinha na tela ganha a largura toda e flui em duas colunas. Painéis com três ou mais filhos e o infográfico de abertura (`data-unidades`) são divisíveis. Cada tela é centrada na área e escalada entre 0,55 e 1,5 (estimativa pela altura natural e correção verificada, sem compensar a largura: com o zoom padrão do navegador as porcentagens já se resolvem no espaço ampliado). O visual herdado em iframe é reduzido por `transform` até 72% da altura do slide, porque o zoom externo não alcança o documento do iframe. A composição é refeita quando a área muda de altura (tela cheia); a linha "A seguir" ocupa espaço em todas as telas para a altura da área não variar entre elas. Episódio de abertura e síntese de fecho têm leiaute próprio de palco (tudo visível, sem abas).

`node scripts/palco/auditoria.mjs <saida.json> [slugs ou todas] [LxA] [pasta de capturas]` percorre as páginas na apresentação (servidor local com base semeada) e pontua cada tela em seis critérios de 0 a 10, com a página recebendo a média das telas: ajuste (25%: rolagem ou corte pela borda), ocupação (20%: altura usada sobre a altura da área, 10 a partir de 66% e 9 a partir de 55%), legibilidade (20%: menor fonte de texto corrido em fração da altura do slide, 10 a partir de 1,9% e 9 a partir de 1,7%, com desconto por linhas acima de 95 caracteres), densidade (15%: palavras na tela, 10 até 100 e 9 até 140), estrutura (10%: cabeça órfã, viúva, legenda solta) e foco (10%: unidades de topo visíveis, 10 até 4). A exigência do projeto é média 9,0 ou mais em toda página e nenhuma tela abaixo de 8; os critérios e a exigência estão na skill `laboratorio-html`. Base de 18/09/2026: antes do compositor, média 8,88 com 83 páginas abaixo de 9 e 55 telas rolando (1400×900); depois, média 9,93 em 1400×900 e 9,91 em 1920×1080, nenhuma página abaixo de 9 (detalhe em `docs/07-relatorio-de-testes.md`). A auditoria deve rodar sozinha (um processo por vez): os visuais herdados em iframe demoram a informar a altura e, sob carga, a medição pode acontecer antes de a tela assentar. Limite conhecido: o visual herdado é reduzido inteiro para caber na caixa de 66% da altura, e em páginas de conteúdo alto (por exemplo c8p11) o texto do iframe fica pequeno; a saída é o porte nativo dessas peças, em andamento capítulo a capítulo.

## 8. Procedimento antes de cada aula

1. Backup.
2. Confirmar data do encontro, link de videoconferência e material publicado.
3. Criar a sessão ao vivo em rascunho e revisar as questões a publicar.
4. Testar a chamada com a própria conta de teste.
